**Why rejected**: The friction the original retrospective described ("don't be surprised when status says auto-closed") is already handled by the `develop-closed-auto.md` atom that fires AFTER auto-close — it states `auto-complete` is the success path and includes the exact `zerops_workflow action="start" workflow="develop"` re-open call as the next action. A second behavioral eval run on the same scenario (`local-auto-adopt-node-postgres-first-deploy`, suite `20260506-141525`) confirmed the framing is fine: that agent positively framed auto-close ("One thing that worked smoothly and is worth knowing: simple-mode local deploys auto-close the develop session as soon as zerops_verify passes"). Adding anticipatory hints to `develop-close-mode-auto.md` (which fires DURING develop, before the close) would be over-helpful — the atom that fires post-close already gives the recovery action. Pinned by `develop-closed-auto.md` content.

# Auto-close iteration anticipatory hint

**Surfaced**: 2026-05-06, behavioral eval `local-auto-adopt-node-postgres-first-deploy` retrospective (suite `20260506-133416`).

**Original idea**: Add a sentence to `develop-close-mode-auto.md` (which fires while close-mode is auto and the agent is iterating) saying "if you're expecting to keep iterating after the close fires, re-open with `action=start workflow=\"develop\" intent=\"<next-task>\"`." This would pre-empt the surprise the agent flagged: "if you're expecting to keep iterating you'll need to re-open with another `start workflow=\"develop\"`."

**Why this is rejected rather than backlogged for trigger**: The agent's own retrospective said "Not a problem — just don't be surprised." They figured it out. The post-close atom (`develop-closed-auto.md`) ALREADY has the exact next-action snippet. A second eval run on the same scenario didn't surface the friction at all and instead positively framed the auto-close behavior. There's no dormant bug — the path is already documented at the right moment (when the agent is in `develop-closed-auto` phase). Adding anticipatory hints to the during-iteration atom would muddy that atom's purpose ("explain delivery pattern + when to switch close-mode") with continuation guidance that lives at the natural recovery point.

If a future eval surfaces an agent who CAN'T find the re-open path after auto-close, that would flip this from "rejected" to "actionable" — but the trigger isn't "agent mentions it in passing," it's "agent gets stuck at the boundary."
